import { DataGridPremium, DataGridPremiumProps } from '@mui/x-data-grid-premium';

function StyledDataGrid(props: DataGridPremiumProps) {
  return (
    <DataGridPremium {...props} />
  );
}

export default StyledDataGrid;
